{
  "name": "photo-request",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Joris Griffioen",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build-next-static": "next build && next export --outdir=build",
    "build": "yarn build-next-static && yarn build-lambda",
    "dev": "netlify dev --command=next --functions=functions"
  },
  "dependencies": {
    "@uploadcare/react-widget": "^1.0.0",
    "axios": "^0.19.0",
    "copy-to-clipboard": "^3.2.0",
    "mongoose": "^5.7.1",
    "netlify-cli": "^2.15.0",
    "next": "^9.0.5",
    "path-to-regexp": "^3.1.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0"
  },
  "devDependencies": {
    "babel-plugin-root-import": "^6.4.1"
  }
}
